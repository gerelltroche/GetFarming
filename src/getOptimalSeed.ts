import { Item, PatchTypes } from "./interfaces/index.interface";


export function getOptimalSeed(farmingLevel: number, patchType: PatchTypes): Item {
    const optimalSeed = {
        amount: 1,
        name: '',
        stackable: false,
        type: 'seed'
    };

    


    return optimalSeed;
}